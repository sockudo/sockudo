# Example environment file for multinode testing
# Copy to .env to use with docker-compose

# Override the sockudo command if needed
# Default: /app/target/debug/sockudo
# Examples:
# SOCKUDO_COMMAND=/app/target/release/sockudo
# SOCKUDO_COMMAND="/app/target/debug/sockudo --config /app/config/custom-config.json"
# SOCKUDO_COMMAND=/usr/local/bin/sockudo
SOCKUDO_COMMAND=/app/target/debug/sockudo