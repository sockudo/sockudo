--- /dev/null
+++ b/ISOLATION.txt
@@ -0,0 +1 @@
+Immutable baseline5613bb291032b6b7660352974b03da9eb0646da0 plus: Only cached enumeration shared-write removal; no S3 singleflight/mutation code. App has_apps live paths are validated separately.
--- a/crates/sockudo-app/src/cached_app_manager.rs
+++ b/crates/sockudo-app/src/cached_app_manager.rs
@@ -387,10 +387,7 @@
 
         if self.settings.enabled {
             self.remember_apps(&apps);
-            for app in &apps {
-                self.cache_app(app).await;
-            }
-            debug!(apps_count = apps.len(), "apps cached");
+            debug!(apps_count = apps.len(), "local app index refreshed");
         }
 
         Ok(apps)
