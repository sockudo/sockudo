Diff in /home/radud/Desktop/Code/Rust/sockudo/crates/sockudo-core/src/redis_client.rs:256:
 /// when configured. Enabling TLS upgrades a `redis://` address to a TLS socket,
 /// so callers do not need to encode certificate behavior in the URL.
 pub async fn build_standalone_client(url: &str, tls: &RedisTlsOptions) -> Result<redis::Client> {
-    let connection_info = url.into_connection_info().map_err(|error| {
-        Error::Redis(format!("failed to parse direct Redis connection: {error}"))
-    })?.set_tcp_settings(TcpSettings::default().set_nodelay(true));
+    let connection_info = url
+        .into_connection_info()
+        .map_err(|error| Error::Redis(format!("failed to parse direct Redis connection: {error}")))?
+        .set_tcp_settings(TcpSettings::default().set_nodelay(true));
 
     if !tls.enabled {
         return redis::Client::open(connection_info)
Diff in /home/radud/Desktop/Code/Rust/sockudo/crates/sockudo-core/src/version_store/memory.rs:718:
     }
 
     async fn active_stream_count(&self, app_id: &str, channel: &str) -> Result<usize> {
-        let handle = self.channels.read().await.get(&Self::channel_key(app_id, channel)).cloned();
-        let Some(handle) = handle else { return Ok(0); };
-        Ok(handle.read().await.messages.values()
+        let handle = self
+            .channels
+            .read()
+            .await
+            .get(&Self::channel_key(app_id, channel))
+            .cloned();
+        let Some(handle) = handle else {
+            return Ok(0);
+        };
+        Ok(handle
+            .read()
+            .await
+            .messages
+            .values()
             .filter_map(|chain| chain.latest.as_ref())
             .filter(|record| record.is_open_ai_stream())
             .count())
Diff in /home/radud/Desktop/Code/Rust/sockudo/crates/sockudo-push/examples/batch_envelope_audit.rs:58:
 
 fn main() {
     let args = std::env::args().collect::<Vec<_>>();
-    let broker_mode = args.get(1).map(String::as_str).filter(|mode| matches!(*mode, "emit" | "verify"));
+    let broker_mode = args
+        .get(1)
+        .map(String::as_str)
+        .filter(|mode| matches!(*mode, "emit" | "verify"));
     for bytes in [256, 65_536] {
         for recipients in [1, 100, 1_000] {
-            if broker_mode.is_some() && (args[2].parse::<usize>().unwrap() != bytes || args[3].parse::<usize>().unwrap() != recipients) { continue; }
+            if broker_mode.is_some()
+                && (args[2].parse::<usize>().unwrap() != bytes
+                    || args[3].parse::<usize>().unwrap() != recipients)
+            {
+                continue;
+            }
             let payload = Arc::new(PushPayload {
                 template_id: None,
                 template_data: sonic_rs::json!({}),
Diff in /home/radud/Desktop/Code/Rust/sockudo/crates/sockudo-push/examples/batch_envelope_audit.rs:98:
             };
             let message = PushQueuePayload::DeliveryBatch(Box::new(batch.clone()));
             if let Some(mode) = broker_mode {
-                if mode == "emit" { std::fs::write(&args[4], encode(&message)).unwrap(); }
-                else { assert_eq!(decode(&std::fs::read_to_string(&args[4]).unwrap()), batch); }
+                if mode == "emit" {
+                    std::fs::write(&args[4], encode(&message)).unwrap();
+                } else {
+                    assert_eq!(decode(&std::fs::read_to_string(&args[4]).unwrap()), batch);
+                }
                 return;
             }
             for rep in 0..7 {
Diff in /home/radud/Desktop/Code/Rust/sockudo/crates/sockudo-push/src/conformance.rs:139:
             device.id = format!("device-{index:03}");
             let selected = index % 7 == 0;
             device.client_id = Some(if selected { "selected" } else { "unrelated" }.to_owned());
-            if selected { expected.insert(device.id.clone()); }
+            if selected {
+                expected.insert(device.id.clone());
+            }
             store.upsert_device(device).await?;
         }
         let mut cursor = None;
Diff in /home/radud/Desktop/Code/Rust/sockudo/crates/sockudo-push/src/conformance.rs:146:
         let mut seen = std::collections::BTreeSet::new();
         loop {
-            let page = store.list_devices_by_client(&app, "selected", 2, cursor).await?;
+            let page = store
+                .list_devices_by_client(&app, "selected", 2, cursor)
+                .await?;
             assert!(page.items.len() <= 2);
             for device in page.items {
                 assert_eq!(device.client_id.as_deref(), Some("selected"));
Diff in /home/radud/Desktop/Code/Rust/sockudo/crates/sockudo-push/src/conformance.rs:152:
                 assert!(seen.insert(device.id));
             }
             cursor = page.next_cursor;
-            if cursor.is_none() { break; }
+            if cursor.is_none() {
+                break;
+            }
         }
         assert_eq!(seen, expected);
         let moved_id = expected.pop_first().unwrap();
Diff in /home/radud/Desktop/Code/Rust/sockudo/crates/sockudo-push/src/conformance.rs:159:
         let mut moved = store.get_device(&app, &moved_id).await?.unwrap();
         moved.client_id = Some("moved".to_owned());
         store.upsert_device(moved).await?;
-        let remaining = store.list_devices_by_client(&app, "selected", 32, None).await?;
-        assert_eq!(remaining.items.into_iter().map(|device| device.id).collect::<std::collections::BTreeSet<_>>(), expected);
+        let remaining = store
+            .list_devices_by_client(&app, "selected", 32, None)
+            .await?;
+        assert_eq!(
+            remaining
+                .items
+                .into_iter()
+                .map(|device| device.id)
+                .collect::<std::collections::BTreeSet<_>>(),
+            expected
+        );
         let moved = store.list_devices_by_client(&app, "moved", 2, None).await?;
         assert_eq!(moved.items.len(), 1);
         assert_eq!(moved.items[0].id, moved_id);
Diff in /home/radud/Desktop/Code/Rust/sockudo/crates/sockudo-push/src/lifecycle/tests.rs:11:
     store: DynPushStore,
     restarted: DynPushStore,
 ) -> PushStorageResult<()> {
-    crate::conformance::PushStoreConformance::assert_indexed_client_pagination(store.as_ref()).await?;
+    crate::conformance::PushStoreConformance::assert_indexed_client_pagination(store.as_ref())
+        .await?;
     let app = format!("lifecycle-{}", rand::random::<u64>());
     let now = crate::pipeline::now_ms();
     let original_idempotency = crate::storage::IdempotencyRecord {
Diff in /home/radud/Desktop/Code/Rust/sockudo/crates/sockudo-push/src/nosql/tests/pagination.rs:79:
             not_before_ms: None,
             expires_at_ms: None,
         };
-        store.append_publish_log_event(PublishLogEvent {
-            app_id: "app".to_owned(), publish_id, event_id: "accepted".to_owned(),
-            occurred_at_ms: 42, intent, fanout_regime: FanoutRegime::FastPath,
-            expected_recipients: 0, fast_threshold: 1, shard_size: 1,
-        }).await.unwrap();
+        store
+            .append_publish_log_event(PublishLogEvent {
+                app_id: "app".to_owned(),
+                publish_id,
+                event_id: "accepted".to_owned(),
+                occurred_at_ms: 42,
+                intent,
+                fanout_regime: FanoutRegime::FastPath,
+                expected_recipients: 0,
+                fast_threshold: 1,
+                shard_size: 1,
+            })
+            .await
+            .unwrap();
     }
     let mut cursor = None;
     let mut seen = std::collections::BTreeSet::new();
Diff in /home/radud/Desktop/Code/Rust/sockudo/crates/sockudo-push/src/nosql/tests/pagination.rs:90:
     loop {
-        let page = store.list_publish_log_events("app", 2, cursor).await.unwrap();
+        let page = store
+            .list_publish_log_events("app", 2, cursor)
+            .await
+            .unwrap();
         assert!(page.items.len() <= 2);
-        for event in page.items { assert!(seen.insert(event.publish_id)); }
+        for event in page.items {
+            assert!(seen.insert(event.publish_id));
+        }
         cursor = page.next_cursor;
-        if cursor.is_none() { break; }
+        if cursor.is_none() {
+            break;
+        }
     }
     assert_eq!(seen.len(), 7);
     assert_eq!(store.backend.full_scans.load(Ordering::Relaxed), 0);
Diff in /home/radud/Desktop/Code/Rust/sockudo/crates/sockudo-server/src/history/mod.rs:389:
 ))]
 mod maintenance_tests;
 
-#[cfg(all(test, any(feature = "postgres", feature = "mysql", feature = "dynamodb", feature = "surrealdb", feature = "scylladb")))]
+#[cfg(all(
+    test,
+    any(
+        feature = "postgres",
+        feature = "mysql",
+        feature = "dynamodb",
+        feature = "surrealdb",
+        feature = "scylladb"
+    )
+))]
 mod presence_live_tests;
 
 #[cfg(all(test, feature = "versioned-messages"))]
